<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle || 'Tableau de bord' %> - Soakoja</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* No sidebar styles needed */
        .main-content {
            padding-top: 70px;
        }
        
        /* Compact Horizontal Navigation Bar - Closer to top */
        .horizontal-nav {
            background: rgba(15, 12, 41, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(139, 92, 246, 0.3);
            border-radius: 8px;
            padding: 0.5rem 1rem;
            margin-bottom: 0.5rem;
            margin-top: 0.5rem;
            box-shadow: 0 4px 16px rgba(139, 92, 246, 0.3);
            position: relative;
            z-index: 10;
            overflow: visible;
        }
        
        /* Light theme navbar styling */
        [data-theme="light"] .horizontal-nav {
            background: rgba(248, 250, 252, 0.8);
            border: 1px solid rgba(124, 58, 237, 0.3);
            box-shadow: 0 4px 16px rgba(124, 58, 237, 0.2);
        }
        
        .horizontal-nav ul {
            list-style: none;
            display: flex;
            gap: 0;
            margin: 0;
            padding: 0;
            align-items: center;
            justify-content: flex-start;
            white-space: nowrap;
            overflow: visible;
            position: relative;
        }
        
        .horizontal-nav li {
            position: relative;
            flex-shrink: 0;
        }
        
        .horizontal-nav .nav-item {
            position: relative;
        }
        
        .horizontal-nav .nav-item > a,
        .horizontal-nav .nav-item > .nav-dropdown {
            display: flex;
            align-items: center;
            gap: 0.4rem;
            padding: 0.6rem 1rem;
            color: var(--text-secondary);
            text-decoration: none;
            border-radius: 6px;
            transition: all 0.2s ease;
            font-weight: 500;
            font-size: 0.9rem;
            border: 1px solid transparent;
            background: transparent;
            cursor: pointer;
            list-style: none;
            margin: 0 1px;
            position: relative;
        }
        
        .horizontal-nav .nav-item > a:hover,
        .horizontal-nav .nav-item > .nav-dropdown:hover {
            background: rgba(139, 92, 246, 0.15);
            color: var(--primary-color);
            border-color: rgba(139, 92, 246, 0.3);
        }
        
        /* Light theme hover states */
        [data-theme="light"] .horizontal-nav .nav-item > a:hover,
        [data-theme="light"] .horizontal-nav .nav-item > .nav-dropdown:hover {
            background: rgba(124, 58, 237, 0.1);
            color: var(--primary-color);
            border-color: rgba(124, 58, 237, 0.3);
        }
        
        .horizontal-nav .nav-item.active > a,
        .horizontal-nav .nav-item > .nav-dropdown:hover {
            background: rgba(139, 92, 246, 0.25);
            color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        /* Light theme active states */
        [data-theme="light"] .horizontal-nav .nav-item.active > a,
        [data-theme="light"] .horizontal-nav .nav-item > .nav-dropdown:hover {
            background: rgba(124, 58, 237, 0.15);
            color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        .horizontal-nav .nav-icon {
            font-size: 0.9rem;
        }
        
        /* Vertical Dropdown Submenu - Positioned closer to menu items */
        .horizontal-nav .submenu {
            position: absolute;
            top: calc(100% + 2px);
            left: 0;
            right: auto;
            min-width: 180px;
            max-width: 250px;
            background: rgba(15, 12, 41, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(139, 92, 246, 0.3);
            border-radius: 6px;
            box-shadow: 0 8px 24px rgba(139, 92, 246, 0.4);
            z-index: 1000;
            list-style: none;
            padding: 0.5rem 0;
            margin: 0;
            opacity: 0;
            visibility: hidden;
            transform: translateY(-5px);
            transition: all 0.15s ease;
            display: block;
            pointer-events: none;
        }
        
        /* Light theme dropdown styling */
        [data-theme="light"] .horizontal-nav .submenu {
            background: rgba(248, 250, 252, 0.95);
            border: 1px solid rgba(124, 58, 237, 0.3);
            box-shadow: 0 8px 24px rgba(124, 58, 237, 0.3);
        }
        
        /* Responsive positioning for dropdowns near screen edges */
        .horizontal-nav .nav-item:nth-last-child(-n+2) .submenu {
            left: auto;
            right: 0;
        }
        
        .horizontal-nav .nav-item:hover .submenu,
        .horizontal-nav .nav-item:focus-within .submenu {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
            pointer-events: auto;
        }
        
        /* Add small gap bridge to prevent losing hover when moving to submenu */
        .horizontal-nav .nav-item::after {
            content: '';
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            height: 4px;
            background: transparent;
            z-index: 999;
        }
        
        .horizontal-nav .submenu li {
            margin: 0;
        }
        
        .horizontal-nav .submenu a {
            display: block;
            padding: 0.5rem 0.75rem;
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 400;
            transition: all 0.2s ease;
            border-radius: 4px;
            margin: 1px 0.25rem;
        }
        
        .horizontal-nav .submenu a:hover {
            background: rgba(139, 92, 246, 0.2);
            color: var(--primary-color);
        }
        
        /* Light theme submenu hover */
        [data-theme="light"] .horizontal-nav .submenu a:hover {
            background: rgba(124, 58, 237, 0.15);
            color: var(--primary-color);
        }
        
        /* Dropdown arrow styling */
        .horizontal-nav .nav-dropdown::after {
            content: '▼';
            font-size: 0.7rem;
            margin-left: 0.3rem;
            transition: transform 0.2s ease;
            opacity: 0.7;
        }
        
        .horizontal-nav .nav-item:hover .nav-dropdown::after {
            transform: rotate(180deg);
        }
        
        /* Responsive horizontal nav */
        @media (max-width: 768px) {
            .horizontal-nav ul {
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
            }
            
            .horizontal-nav .nav-item > a,
            .horizontal-nav .nav-item > details > summary {
                padding: 0.5rem 0.75rem;
                font-size: 0.85rem;
            }
            
            .horizontal-nav .nav-icon {
                font-size: 0.8rem;
            }
            
            .horizontal-nav .submenu {
                min-width: 140px;
            }
            
            .horizontal-nav .submenu a {
                padding: 0.4rem 0.6rem;
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <script>
        // Theme Switcher Functionality
        function toggleTheme() {
            const currentTheme = document.documentElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'light' ? 'dark' : 'light';
            
            document.documentElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            
            // Update theme icon
            const themeIcon = document.getElementById('theme-icon');
            if (themeIcon) {
                if (newTheme === 'light') {
                    themeIcon.className = 'fas fa-moon theme-icon';
                } else {
                    themeIcon.className = 'fas fa-sun theme-icon';
                }
            }
        }
        
        // Load saved theme on page load
        function loadTheme() {
            const savedTheme = localStorage.getItem('theme') || 'dark';
            document.documentElement.setAttribute('data-theme', savedTheme);
            
            // Update theme icon
            const themeIcon = document.getElementById('theme-icon');
            if (themeIcon) {
                if (savedTheme === 'light') {
                    themeIcon.className = 'fas fa-moon theme-icon';
                } else {
                    themeIcon.className = 'fas fa-sun theme-icon';
                }
            }
        }
        
        // Load theme immediately
        loadTheme();
        
        // Also load theme when DOM is ready
        document.addEventListener('DOMContentLoaded', loadTheme);
    </script>
    <!-- Inclusion du header/navigation -->
    <%- include('partials/header') %>

    <!-- Horizontal Navigation Bar -->
    <% if (typeof user !== 'undefined' && user) { %>
    <div class="main-content">
        <div class="container" style="overflow: visible;">
            <nav class="horizontal-nav">
                <ul>
                    <li class="nav-item <%= typeof pageTitle !== 'undefined' && pageTitle === 'Tableau de bord' ? 'active' : '' %>">
                        <a href="/dashboard">
                            <i class="fas fa-chart-line nav-icon"></i>
                            <span>Tableau de bord</span>
                        </a>
                    </li>
                    
                    <li class="nav-item">
                        <span class="nav-dropdown">
                            <i class="fas fa-calendar-plus nav-icon"></i>
                            <span>Plannings</span>
                        </span>
                        <ul class="submenu">
                            <li><a href="/plannings/saisie">Saisie</a></li>
                            <li><a href="/plannings/livraison">Livraison</a></li>
                            <li><a href="/plannings/avances">Avances</a></li>
                            <li><a href="/plannings/retour-livraison">Retour livraison</a></li>
                            <li><a href="/plannings/retour-avances">Retour avances</a></li>
                            <li><a href="/plannings/suivi-avance">Suivi avance</a></li>
                        </ul>
                    </li>

                    <li class="nav-item">
                        <span class="nav-dropdown">
                            <i class="fas fa-credit-card nav-icon"></i>
                            <span>Paiement</span>
                        </span>
                        <ul class="submenu">
                            <li><a href="/paiement/saisie">Saisie</a></li>
                            <li><a href="/paiement/suivi">Suivi</a></li>
                        </ul>
                    </li>

                    <li class="nav-item">
                        <span class="nav-dropdown">
                            <i class="fas fa-chart-bar nav-icon"></i>
                            <span>Relevés</span>
                        </span>
                        <ul class="submenu">
                            <li><a href="/releves/saisie">Saisie</a></li>
                            <li><a href="/releves/calcul">Calcul facturation</a></li>
                            <li><a href="/releves/encaissement">Encaissement</a></li>
                        </ul>
                    </li>

                    <% if (user.niveauMenu >= 4) { %>
                    <li class="nav-item">
                        <span class="nav-dropdown">
                            <i class="fas fa-users-cog nav-icon"></i>
                            <span>Administration</span>
                        </span>
                        <ul class="submenu">
                            <li><a href="/admin/employes">Employés</a></li>
                            <li><a href="/admin/ouvrages">Ouvrages</a></li>
                            <li><a href="/admin/salaires">Salaires</a></li>
                        </ul>
                    </li>
                    <% } %>
                </ul>
            </nav>
        <div class="container">
            <div class="page-header">
                <h1><%= pageTitle || 'Tableau de bord' %></h1>
                <p>Bienvenue sur la plateforme de gestion HydriaV2.</p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger">
                    <strong>Erreur :</strong> <%= error %>
                </div>
            <% } %>

            <h2>Statistiques Clés</h2>
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>🏗️ Ouvrages Actifs</h3>
                        <p class="stat-value"><%= stats.ouvrages_actifs %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>👥 Employés</h3>
                        <p class="stat-value"><%= stats.employes_actifs %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>📋 Plannings en cours</h3>
                        <p class="stat-value"><%= stats.plannings_en_cours %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>🏘️ Villages</h3>
                        <p class="stat-value"><%= stats.villages_total %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>💧 Points d'eau</h3>
                        <p class="stat-value"><%= stats.points_eau_actifs %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>🏢 Bases</h3>
                        <p class="stat-value"><%= stats.bases_total %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>📍 Communes</h3>
                        <p class="stat-value"><%= stats.communes_total %></p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-content">
                        <h3>✅ Plannings validés</h3>
                        <p class="stat-value"><%= stats.plannings_valides %></p>
                    </div>
                </div>
            </div>

            <h2>Accès Rapides</h2>
            <div class="menu-grid">
                <a href="/plannings/saisie" class="menu-card">
                    <div class="menu-icon">📝</div>
                    <h3>Saisie Planning</h3>
                    <p>Créer ou modifier un planning d'activité.</p>
                </a>
                <a href="/plannings/livraison" class="menu-card">
                    <div class="menu-icon">🚚</div>
                    <h3>Livraison Matériel</h3>
                    <p>Gérer les livraisons de matériel pour les activités.</p>
                </a>
                <a href="/releves/saisie" class="menu-card">
                    <div class="menu-icon">📊</div>
                    <h3>Relevés</h3>
                    <p>Saisir les relevés de consommation.</p>
                </a>
                <% if (user.niveauMenu >= 4) { %>
                <a href="/admin/employes" class="menu-card">
                    <div class="menu-icon">⚙️</div>
                    <h3>Administration</h3>
                    <p>Gérer les utilisateurs et les paramètres.</p>
                </a>
                <% } %>
            </div>

            <h2 style="margin-top: 3rem;">Derniers Plannings</h2>
            <div class="table-container">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Activité</th>
                            <th>Employé</th>
                            <th>Statut</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (plannings_recents && plannings_recents.length > 0) { %>
                            <% plannings_recents.forEach(planning => { %>
                                <tr>
                                    <td><%= new Date(planning.Date).toLocaleDateString('fr-FR') %></td>
                                    <td><%= planning.Activite ? planning.Activite.Nom : 'N/A' %></td>
                                    <td><%= planning.Employe ? `${planning.Employe.Prenom} ${planning.Employe.Nom}` : 'N/A' %></td>
                                    <td>
                                        <% if (planning.EstValide) { %>
                                            <span class="badge badge-success">✅ Validé</span>
                                        <% } else { %>
                                            <span class="badge badge-warning">⏳ En attente</span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <a href="/plannings/view/<%= planning.ID %>" class="btn btn-sm btn-info">Voir</a>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" class="empty-state">Aucun planning récent à afficher.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>

            <div class="footer-info">
                <p>Dernière connexion : <%= new Date().toLocaleString('fr-FR') %></p>
                <p>Version : HydriaV2 - 2024</p>
            </div>
        </div>
    </div>
    <% } else { %>
    <div class="main-content">
        <div class="container">
            <div class="page-header">
                <h1>Accès restreint</h1>
                <p>Veuillez vous connecter pour accéder au tableau de bord.</p>
            </div>
        </div>
    </div>
    <% } %>
</body>
</html>