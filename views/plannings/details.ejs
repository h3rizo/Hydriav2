<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> - Soakoja</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .details-grid {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 2rem;
        }
        .planning-info {
            background: var(--bg-secondary);
            padding: 1.5rem;
            border-radius: 10px;
            box-shadow: 0 5px 15px var(--shadow-color);
        }
        .planning-info h3 {
            border-bottom: 2px solid var(--border-color);
            padding-bottom: 0.5rem;
            margin-bottom: 1rem;
        }
        .info-item {
            margin-bottom: 0.75rem;
        }
        .info-item strong {
            display: block;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }
        .form-container {
            background: var(--bg-secondary);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 5px 15px var(--shadow-color);
        }
        textarea {
            width: 100%;
            min-height: 150px;
            resize: vertical;
        }
        .form-actions {
            display: flex;
            justify-content: flex-end;
            gap: 1rem;
            margin-top: 1.5rem;
        }
        .message-box {
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1.5rem;
            font-weight: bold;
        }
        .success-box {
            background-color: #d4edda;
            color: #155724;
            border-left: 5px solid #c3e6cb;
        }
        .error-box {
            background-color: #f8d7da;
            color: #721c24;
            border-left: 5px solid #f5c6cb;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %>

    <div class="container">
        <div class="page-header">
            <h1><%= pageTitle %></h1>
            <a href="/plannings/kanban" class="btn btn-secondary">← Retour au Kanban</a>
        </div>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="message-box success-box"><%= success %></div>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="message-box error-box"><%= error %></div>
        <% } %>

        <div class="details-grid">
            <aside class="planning-info">
                <h3>Informations du Planning</h3>
                <div class="info-item"><strong>Date:</strong> <span><%= new Date(planning.date).toLocaleDateString('fr-FR') %></span></div>
                <div class="info-item"><strong>Employé:</strong> <span><%= planning.Employe ? `${planning.Employe.prenom} ${planning.Employe.nom}` : 'N/A' %></span></div>
                <div class="info-item"><strong>Ouvrage:</strong> <span><%= planning.Ouvrage ? planning.Ouvrage.nom : 'N/A' %></span></div>
                <div class="info-item"><strong>Activité:</strong> <span><%= planning.Activite ? planning.Activite.nom : 'N/A' %></span></div>
                <div class="info-item"><strong>Remarque initiale:</strong> <span><%= planning.remarque || '-' %></span></div>
                 <div class="info-item"><strong>Statut:</strong> <span class="status <%= planning.estvalide ? 'validated' : 'pending' %>"><%= planning.estvalide ? 'Validé' : 'En attente' %></span></div>
            </aside>

            <main class="form-container">
                <form method="POST" action="/plannings/<%= planning.id %>/details">
                    <div class="form-group">
                        <label for="bilan"><h3>Bilan de l'activité</h3></label>
                        <textarea id="bilan" name="bilan" rows="8" placeholder="Décrivez en détail le bilan de l'intervention..."><%= details.bilan || '' %></textarea>
                    </div>
                    <div class="form-group" style="margin-top: 1.5rem;">
                        <label for="suiteadonner"><h3>Suite à donner</h3></label>
                        <textarea id="suiteadonner" name="suiteadonner" rows="8" placeholder="Quelles sont les prochaines étapes ? Y a-t-il un suivi à faire ?"><%= details.suiteadonner || '' %></textarea>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Enregistrer les détails</button>
                    </div>
                </form>
            </main>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>