<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erreur - Soakoja</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .error-container {
            text-align: center;
            padding: 4rem 2rem;
        }
        .error-container h1 {
            font-size: 3rem;
            color: #dc3545;
            margin-bottom: 1rem;
        }
        .error-container p {
            font-size: 1.2rem;
            color: var(--text-secondary);
            margin-bottom: 2rem;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar', { user: user || null }) %>
    <div class="container">
        <div class="error-container">
            <h1>Oops! Une erreur est survenue.</h1>
            <p><%= message || "Une erreur inattendue s'est produite sur le serveur." %></p>
            <a href="/dashboard" class="btn btn-primary">Retour au tableau de bord</a>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>
</html>